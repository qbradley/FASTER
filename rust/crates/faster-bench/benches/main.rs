use criterion::criterion_main;

// Benchmark groups will be registered here as the implementation grows.
// Example:
//   criterion_group!(benches, bench_upsert, bench_read);
//   criterion_main!(benches);

fn main() {
    println!("FASTER benchmarks — no benchmark groups registered yet.");
    println!("Add criterion_group! and criterion_main! as implementation progresses.");
}
