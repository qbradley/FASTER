[package]
name = "disk-io-bench"
version = "0.1.0"
description = "Disk I/O benchmark comparing FASTER device implementations"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[features]
default = []
io_uring = ["faster-uring/io_uring"]

[dependencies]
faster-core = { path = "../../faster-core" }
faster-tokio = { path = "../../faster-tokio" }
faster-uring = { path = "../../faster-uring", optional = true }
clap = { version = "4", features = ["derive"] }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { version = "1", features = [
    "rt-multi-thread",
    "sync",
    "time",
    "macros",
] }
rand = "0.9"
