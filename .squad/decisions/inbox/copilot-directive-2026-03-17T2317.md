### 2026-03-17T23:17: User directive
**By:** qbradley (via Copilot)
**What:** When we fix a bug we should *always* add a regression test that fails if the bug regresses.
**Why:** User request — captured for team memory. Ensures every fix is validated and regressions are caught automatically.
