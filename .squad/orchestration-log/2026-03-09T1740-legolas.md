# Legolas Spawn: Fix hash_layout_bench (Dead Code + Dataset Sizes)
**Timestamp:** 2026-03-09T1740  
**Agent:** Legolas (Performance Guru)  
**Mode:** background  
**Model:** claude-opus-4.6  
**Status:** SUCCESS

## Task
Fix `hash_layout_bench` — it was taking 1+ hour in debug mode and many hours in release. The user had disabled it by commenting out the `benches()` call. Diagnose root cause, clean up dead code, reduce dataset sizes, add Criterion tuning, and re-enable `benches()`.

## Routing Rationale
Legolas owns all benchmark infrastructure and is the performance expert. They have full context on the Criterion setup, bench file patterns, and the prior nextest hang fix. This is a direct continuation of their benchmarking work.

## Execution Summary

| Field | Value |
|-------|-------|
| **Agent routed** | Legolas (Performance Guru) |
| **Why chosen** | Owns bench infrastructure; authored the existing bench files; direct continuation of Wave 3 bench work |
| **Mode** | `background` |
| **Why this mode** | Background bench fix with no hard dependencies on the documentation agents (Gandalf, Aragorn) in this wave |
| **Files authorized to read** | `rust/benches/hash_layout_bench.rs`, `rust/benches/ycsb.rs`, `rust/benches/core_benchmarks.rs` |
| **File(s) agent must produce** | `rust/benches/hash_layout_bench.rs` (primary fix) |
| **Outcome** | Completed — committed on `legolas/fix-hash-bench`, merged to `squad` |

## Root Cause
`hash_layout_bench.rs` contained 206 lines of dead Open Addressing (OA) prototype code — a discarded layout experiment never removed. This prototype ran massive dataset sizes through Criterion's full benchmark harness even in smoke-test mode.

## Changes Made

| Change | Detail |
|--------|--------|
| Removed dead OA prototype code | -206 lines of discarded layout experiment |
| Reduced dataset sizes | Benchmark datasets scaled to realistic sizes for CI runs |
| Added Criterion tuning | `measurement_time`, `warm_up_time`, `sample_size` configured for fast iteration |
| Re-enabled `benches()` call | The benchmarks were disabled; now re-enabled with safe runtimes |

## Verification

| Metric | Before | After |
|--------|--------|-------|
| Smoke test runtime | >1 hour (debug) | **0.15s** |
| `benches()` status | Disabled (commented out) | **Re-enabled** |
| Dead code lines | 206 | **0** |

## Team Impact
- **All agents:** `hash_layout_bench` is now healthy and included in smoke tests
- **CI:** Bench smoke test is fast and safe for pre-merge
- **Gandalf:** TESTING-ARCHITECTURE.md benchmark tier now has a clean bench file to reference

## Commit
- Branch: `legolas/fix-hash-bench`
- Merged to: `squad`
