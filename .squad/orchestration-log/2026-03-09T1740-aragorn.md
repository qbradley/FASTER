# Aragorn Spawn: Mutation Testing Setup (cargo-mutants)
**Timestamp:** 2026-03-09T1740  
**Agent:** Aragorn (Rust Expert)  
**Mode:** background  
**Model:** claude-opus-4.6  
**Status:** SUCCESS

## Task
Set up `cargo-mutants` mutation testing infrastructure for the FASTER Rust project. Configure `mutants.toml`, run a pilot on `address.rs` to validate catch rate and timeout behavior, and write `docs/mutation-testing.md` as a workflow guide.

## Routing Rationale
Aragorn owns Rust implementation quality and unsafe code correctness. Mutation testing directly validates test suite effectiveness for the core modules Aragorn maintains. Aragorn also has prior context on the address.rs layout and the timeout issues that plagued earlier attempts.

## Execution Summary

| Field | Value |
|-------|-------|
| **Agent routed** | Aragorn (Rust Expert) |
| **Why chosen** | Owns mutation testing plan from `.squad/plans/mutation-testing-plan.md`; deep knowledge of address.rs field-packing patterns that caused prior timeouts |
| **Mode** | `background` |
| **Why this mode** | Isolated infrastructure task; no concurrent file conflicts with other wave agents |
| **Files authorized to read** | `rust/mutants.toml`, `rust/src/address.rs`, `.squad/plans/mutation-testing-plan.md`, existing test files |
| **File(s) agent must produce** | `rust/mutants.toml` (update), `rust/docs/mutation-testing.md` (new), `rust/.gitignore` (update) |
| **Outcome** | Completed — committed on `aragorn/mutation-testing`, merged to `squad` |

## Deliverables

- **`rust/mutants.toml`** — cargo-mutants v27.0 configuration
- **`rust/docs/mutation-testing.md`** — workflow guide with pilot results
- **`rust/.gitignore`** — Added `mutants.out/` exclusion

## Pilot Results: `address.rs`

| Metric | Value |
|--------|-------|
| Viable mutants | 55 |
| Caught | 55 (100%) |
| Missed | 0 |
| Unviable | 9 |
| Timeouts | 0 |
| Runtime | ~6 min |

## Key Configuration Decisions

| Decision | Rationale |
|----------|-----------|
| `timeout_multiplier = 3.0` | ~120s headroom; eliminates prior timeout issues (prior 8 timeouts resolved) |
| `test_tool = "nextest"` | Matches CI runner; parallel test execution |
| `exclude_re` for field-packing | `page_index`, `offset_in_page`, `from_page_offset` cause infinite loops in unsafe bit ops |
| `exclude_re` for Display/Debug | Surviving cosmetic mutants are noise, not gaps |
| `examine_globs` for Phase 1 modules | Focus on high-value code; full-crate deferred to periodic audits |
| Quality target: 80% all / 85% critical | Pilot exceeded at 100% |

## Team Impact
- **All agents:** Can run `cargo mutants --package faster-core -F 'src/myfile.rs'` to check mutation coverage on changed files
- **CI (future):** Full-scope run as pre-merge gate when runtime is acceptable (<30 min target)
- **Gandalf:** Mutation testing gap documented in `TESTING-ARCHITECTURE.md` is now resolved by this work

## Verification
- Committed on `aragorn/mutation-testing`, merged to `squad`
- `mutants.out/` confirmed gitignored
- 0 timeouts validates `exclude_re` for field-packing operations
